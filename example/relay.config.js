module.exports = {
  src: './src', // Root directory of application code.
  schema: './schema.graphql', // Path to schema.graphql.
  language: 'typescript', // The name of the language plugin used for input files and artifacts.
};
